// JavaScript script
// that updates the text color of
// the <header> element to red (#FF0000)

window.addEventListener('load', function () {
    const header = document.querySelector('header');
    header.style.color = '#FF0000';
});